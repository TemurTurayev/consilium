# Override the WebSocket endpoint the UI connects to.
# The default (no .env needed) matches `consilium serve --addr 127.0.0.1:7878`.
# Copy this file to `.env` and edit the line to point elsewhere.
VITE_WS_URL=ws://localhost:7878/ws/session
