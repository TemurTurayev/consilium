{
  "name": "add-greeting",
  "prompt": "Add a public function `greeting() -> String` to src/lib.rs that returns the string \"hello\". A test in the same file already asserts this — make it pass without changing the test.",
  "context": "A single-crate Rust library. Run `cargo test` to check your work.",
  "verify": { "build": "cargo build", "test": "cargo test" }
}
